# Macaulay2

Self-initializing types, methods, typed dispatch, hash and cache tables,
regular expressions, recursion, loops, control flow, and tree transformations.

```m2
{{#include ../snippets/macaulay2.m2}}
```
